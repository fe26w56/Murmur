# 04-screen-design.md 再レビュー（2026-02-16）

対象ファイル: `docs/04-screen-design.md`

## 再レビュー結果

### 前回指摘の対応確認

1. S1aの責務分離明記
- 状態: ⚠️ 部分対応（新たな矛盾あり）

## 新規指摘

### 1. S1aの責務定義が同一ファイル内で矛盾
- 重要度: Major
- 該当箇所: `docs/04-screen-design.md:58`, `docs/04-screen-design.md:266`
- 内容:
  - 画面一覧では「`route.ts` がセッション交換・リダイレクト処理」を担当と記載。
  - S1a詳細では「`route.ts` は不使用、`page.tsx` でセッション交換・リダイレクトを実施」と記載。
- 影響: 実装担当がOAuthコールバックの実装方式を一意に決められず、認証不具合または実装手戻りの原因になる。
- 修正案:
  - 方針を1つに統一する（`route.ts`方式 or `page.tsx`方式）。
  - 画面一覧とS1a詳細の両方を同じ記述にそろえる。

## 結論

S1a以外は整合している。先にS1aの実装方針を一意化してから実装に進むのが安全。

## 対応状況

- [x] 指摘1: 画面一覧（58行目）の概要欄を「`page.tsx` で完結」に統一。S1a詳細（265-266行目）の記述と整合
